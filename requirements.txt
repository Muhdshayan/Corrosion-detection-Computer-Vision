# ============================================================
#  Corrosion Detection — Instance Segmentation Pipeline
#  XIS AI Department Technical Assessment
#
#  Environment: Google Colab (Python 3.10, CUDA GPU)
#
#  Usage:
#    pip install -r requirements.txt
#
#  Note: torch and torchvision are pre-installed on Colab
#  with CUDA support. The only package you need to install
#  manually in Colab is pycocotools (Cell 3 of the notebook).
# ============================================================

# Deep learning framework
torch>=2.1.0
torchvision>=0.16.0

# Image processing
Pillow>=9.5.0
numpy>=1.24.0

# COCO annotation utilities (must be installed manually on Colab)
pycocotools>=2.0.7

# Visualisation
matplotlib>=3.7.0

# Progress bars
tqdm>=4.66.0
